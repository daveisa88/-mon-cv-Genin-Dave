import React from "react";

const Navbar = () => {
    return (
        <nav className="bg-blue-900 text-white py-2 text-center">
            <p>Cette application a été créée par un utilisateur de Google Apps Script</p>
        </nav>
    );
};

export default Navbar;
